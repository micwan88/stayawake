# Stay Awake

A tool to keep your computer awake. 
Of course, also keep your `Microsoft Teams`'s status `Active`.

Usage:
```bash
#Update python runtime first
python -m pip install --upgrade pip

#Install uv runtime
python -m pip install uv

#Run stayawake tool
python -m uv run --locked main.py
```